name: scripts

on:
  push:
    branches: [main]
    paths:
      - 'scripts/**'
      - '.github/workflows/scripts.yml'
      # devstack.sh reads the server's log messages, and devstack-test.sh
      # asserts they still exist. A rename in server/ has to run this job.
      - 'server/**'
  pull_request:
    paths:
      - 'scripts/**'
      - '.github/workflows/scripts.yml'
      - 'server/**'

# Least privilege: lint and a self-contained test.
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  lint-test:
    name: shellcheck + devstack tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: shellcheck
        run: shellcheck scripts/*.sh
      - name: devstack tests
        # Starts no containers; the fixtures it needs are a bash it can copy
        # and a python listener.
        run: scripts/devstack-test.sh

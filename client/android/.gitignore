# NB: the Gradle wrapper (gradlew, gradlew.bat, gradle/wrapper/gradle-wrapper.jar)
# is intentionally NOT ignored — it must be committed so fresh clones and CI can
# run Gradle / `flutter build apk` without a system Gradle install.
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
